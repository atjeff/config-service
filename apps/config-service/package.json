{
  "name": "@repo/config-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy src/server.ts",
    "deploy:prod": "wrangler deploy src/server.ts --env=prod",
    "dev": "wrangler dev src/server.ts --port=60101 --inspector-port=9101 --show-interactive-dev-session=false"
  },
  "dependencies": {
    "@unkey/cache": "^1.3.0",
    "superjson": "^2.2.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  }
}
